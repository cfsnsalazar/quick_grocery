<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quickgrocery">

  <application
      android:name=".common.application.QuickGroceryApplication"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/LightTheme">
    <activity android:name=".meals.activity.MealsActivity"
        android:configChanges="uiMode">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".shoppingList.activity.ShoppingListActivity"
        android:configChanges="uiMode"/>
    <activity android:name=".settings.activity.SettingsActivity"
        android:configChanges="uiMode"/>
  </application>

</manifest>